<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cain.tencent.com.androidexercisedemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FrescoActivity" />
        <activity android:name=".BitmapActivity" />
        <activity android:name=".TipsActivity" />
        <activity android:name=".RxJavaActivity" />
        <activity android:name=".Empty1Activity" />
        <activity android:name=".Empty2Activity" />
        <activity android:name=".AnimatorActivity" />
        <activity android:name=".OptimizeLayoutActivity"></activity>
    </application>

</manifest>